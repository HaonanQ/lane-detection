#### 邱浩楠（组长）220405313

- **负责模块**：车道线检测算法核心实现
- 具体代码范围
  - lanedetection2.py 中 `LaneDetection` 类的实现
  - 边缘检测（`_edge_canny`）、ROI 提取（`_roi_trapezoid`）、霍夫变换（`_Hough_line_fitting`）、直线拟合（`_lane_line_fitting`）等核心算法
  - 确保算法参数正确初始化与调用

```python
# 核心代码示例
class LaneDetection:
    def __init__(self, ...):
        # 所有参数初始化（现有代码）
        ...

    def __call__(self, img):
        # 检测流程串联（现有代码）
        ...

    def _white_balance(self, img):
        # 白平衡实现（现有代码）
        ...

    # 其他所有算法方法的实现（现有代码）
    ...
```

#### 张真维 220405321

- **负责模块**：UI 界面布局与基础控件实现
- 具体代码范围
  - mainwindow.py 中 `setupUi` 方法（所有 UI 控件的创建与布局）
  - mainwindow.py 中 `retranslateUi` 方法（控件文本翻译与初始化）
  - 确保界面控件（标签、按钮、显示区域）的位置与尺寸正确

```python
# 核心代码示例（张真维负责部分）
def setupUi(self, MainWindow):
    MainWindow.setObjectName("MainWindow")
    MainWindow.resize(1000, 600)
    MainWindow.setFixedSize(1000, 600)
    self.centralwidget = QWidget(MainWindow)
    # 所有标签、按钮的创建与几何位置设置（现有代码）
    ...

def retranslateUi(self, MainWindow):
    _translate = QCoreApplication.translate
    MainWindow.setWindowTitle(_translate("MainWindow", "车道线检测"))
    # 所有控件文本设置（现有代码）
    ...
```

#### 赵浩然 220405302

- **负责模块**：图像 / 视频显示逻辑

- `LaneDetection` 类预处理（`_white_balance`、`_image_preprocess`）,白平衡处理算法

- 
- mainwindow.py 中 `show_input_image` 与 `show_output_image` 方法（图片格式转换与显示）
  - mainwindow.py 中 `show_input_video` 与 `show_output_video` 方法（视频帧读取与显示）
- 确保 OpenCV 图像格式与 PyQt 显示格式正确转换

```python
# 核心代码示例（赵浩然负责部分）
def show_input_image(self, image):
    img = cv2.resize(image, (self.label_inputs.size().height(), self.label_inputs.size().width()))
    img = cv2.cvtColor(img, cv2.COLOR_BGR2RGB)
    QImg = QImage(img.data, img.shape[1], img.shape[0], img.shape[1] * 3, QImage.Format_RGB888)
    self.label_inputs.setPixmap(QPixmap.fromImage(QImg))

def show_output_image(self, image):
    # 与show_input_image类似的实现（现有代码）
    ...

def show_input_video(self):
    # 视频帧读取与显示逻辑（现有代码）
    ...
```

#### 陈书俊 220405319

- **负责模块**：车道线检测算法的调用并展示
- 具体代码范围
mainwindow.py 中 Ui_MainWindow 类的 __init__ 方法（初始化核心变量与类实例）
mainwindow.py 中 open 方法（文件格式判断、数据加载核心逻辑）
mainwindow.py 中 run 方法（调用检测算法并分发处理结果）
- 视频帧信号的处理
- 收集数据
### 协作说明

1. **接口约定**：组长提前定义好 mainwindow.py 与 lanedetection2.py 的交互接口（如 `LaneDetection` 类的调用方式、图像数据格式）
2. **代码合并**：基于现有代码结构，每人提交各自负责模块的代码，组长负责最终整合，确保无冲突
3. **职责边界**：严格按照上述代码范围分工，不交叉修改其他模块代码，如需调整需组内协商
4. **测试分工**：各自测试负责模块的功能（如 UI 布局是否正常、图像显示是否正确、算法输出是否合理）